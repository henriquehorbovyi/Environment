import requests
from bs4 import BeautifulSoup

url = "https://developer.android.com/studio/?hl=pt-br"
page = requests.get(url)
soup = BeautifulSoup(page.text, 'html.parser')

links = soup.find_all('a', class_='devsite-dialog-button')

#for link in links:
linux_version = links.find(text='Download Android Studio For Linux')
print(links)
